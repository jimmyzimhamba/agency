# Copy this file to security-tests.env (same folder) and fill in the one
# secret value below. NEVER paste your service_role key into chat, an email,
# or anywhere else — this local file is the only place it should live, and
# it should never be uploaded, committed, or shared with anyone.
#
# How to get it:
#   1. Open your Supabase project dashboard.
#   2. Project Settings (gear icon, bottom left) -> API.
#   3. Under "Project API keys", find the one labeled "service_role" /
#      "secret" (NOT "anon" / "public" — those are already public and safe;
#      the service_role one is the powerful, must-stay-secret one).
#   4. Click reveal, copy it, and paste it below after the = sign.

SUPABASE_URL=https://hzjadsomvosoolxtnskj.supabase.co
SUPABASE_ANON_KEY=sb_publishable_jQYkXfZ0aaj03MGkf6Bk9A_yqDztzsJ
SUPABASE_SERVICE_ROLE_KEY=paste-your-service-role-secret-key-here
